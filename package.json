{
  "name": "schema-issue",
  "type": "module",
  "scripts": {
    "start": "bunx --bun primate",
    "build": "bunx --bun primate build",
    "serve": "bunx --bun primate serve",
    "dev": "bun run start",
    "mi:gen": "npx primate migrate:create",
    "mi:status": "npx primate migrate:status",
    "mi:apply": "npx primate migrate:apply"
  },
  "dependencies": {
    "@primate/postgresql": "^0.6.0",
    "@primate/svelte": "^0.23.0",
    "primate": "^0.37.0",
    "svelte": "^5.55.4"
  },
  "devDependencies": {
    "@plsp/svelte": "^0.1.0",
    "typescript": "^6.0.3"
  }
}
